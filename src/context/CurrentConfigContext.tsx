import { createContext } from "react";
import { CurrentConfigContextInterface } from "../interfaces/interfaces";

export const CurrentConfigContext = createContext({} as CurrentConfigContextInterface);